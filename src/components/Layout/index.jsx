import Message from '../Message'
import Form from '../Form'
import Balance from '../Balance'

const Layout = () => {
    return (
        <main>
            <Message />
            <Form />
            <Balance />
        </main>
    )
}

export default Layout